// Connect to PG, handle toggles, populate sidebar, etc.

document.getElementById('query').addEventListener('click', function() {
    // Create SQL query based on checked boxes
    
    // Execute query and fetch data from `items` table
    
    // Render the fetched data in the main content area
});
